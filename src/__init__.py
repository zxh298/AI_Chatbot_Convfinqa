"""ConvFinQA prototype package.

The package is organized around a simple pipeline:
load a known ConvFinQA record, generate model predictions, save raw run rows,
then evaluate those saved predictions against `executed_answers`.
"""
